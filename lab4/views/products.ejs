<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>Список товарів</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
<h1>Список усіх товарів</h1>
<% if (products.length > 0) { %>
    <ul>
        <% products.forEach(product => { %>
            <li>
                <strong><%= product.name %></strong> - <%= product.description %> - Ціна: <%= product.price %> грн
            </li>
        <% }); %>
    </ul>
<% } else { %>
    <p>Товарів немає.</p>
<% } %>
<a href="/">На головну</a>
</body>
</html>
